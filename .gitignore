# Python / IDE
.venv/
.idea/
__pycache__/

# Node
node_modules/
dist/
*.local
# vitest JSON summary, written only under CI (vitest.config.ts) for tools/ci-assert-suites.mjs
test-results.json

# ── reference/: EVERYTHING local-only lives here and only here ──────────────
# Course material (PDFs, task .txt files, Variablenliste, videos, photos), the
# research corpus (extractions, OCR evidence, video frames), the solution
# oracles (Claude_work/, solutions.md, weichen_video.md, DOMAIN_MODEL.md) and
# the internal HANDOFF. The publish rule is structural: the tracked tree IS the
# publishable tree — see reference/README.md and ARCHITECTURE.md §9.4.
# tests/oracle/no-bundle.test.ts asserts git ignores this folder completely.
reference/

# Belt and braces: course-document types never enter the tree anywhere else.
*.pdf
*.avi
*.mp4
*.mkv
*.docx
*.pptx
*.jpg
*.jpeg

# OS
Thumbs.db
Desktop.ini
